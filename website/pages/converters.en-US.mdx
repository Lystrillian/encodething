This is an inline counter directly created in MDX:

import React from "react"
import { useTextReverse } from '../hooks/useTextReverse.ts'

export const initialCounterState = 4;

export const MyCounter = () => {
  const [counter, setCounter] = React.useState(initialCounterState)
  
  const handleInputChange = (e) => {
    const text = e.target.value;
    setInputText(text);
    reverseText(text);
  };

  return (
    <>
    <button onClick={() => setCounter((c) => c + 1)}>
      Increment {counter}
    </button>
      <p>Reversed Text: {textReverse}</p>
      <textarea value={inputText} onChange={handleInputChange} />
    </>
  );
};

<MyCounter />

That's cool isn't it../hooks/string/useTextReverse.ts